import { useState,type FormEvent } from 'react';
import { httpsCallable } from 'firebase/functions';
import { Navigate,useNavigate } from 'react-router-dom';
import { useAuth } from '../auth/AuthProvider';
import { functions } from '../firebase/client';
export function OnboardingPage(){const {user,membership,refreshToken}=useAuth();const navigate=useNavigate();const [error,setError]=useState(''),[busy,setBusy]=useState(false);if(!user?.emailVerified)return <Navigate to="/verify-email" replace/>;if(membership)return <Navigate to="/" replace/>;async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const d=Object.fromEntries(new FormData(e.currentTarget));try{if(!functions)throw new Error('Firebase is not configured.');const legal=JSON.parse(sessionStorage.getItem('stockguard-legal-acceptance')||'{}') as {terms?:boolean;privacy?:boolean};await httpsCallable(functions,'completeOnboarding')({...d,employeeCount:Number(d.employeeCount),termsAccepted:Boolean(legal.terms),privacyAccepted:Boolean(legal.privacy)});await refreshToken();navigate('/',{replace:true})}catch(err){setError(err instanceof Error?err.message:'Could not create the business.')}finally{setBusy(false)}}return <main className="auth-page"><section className="auth-brand"><span className="brand-mark">SG</span><h1>Tell us about your business.</h1><p>South African defaults are applied automatically and can be customised later.</p></section><section className="auth-panel"><form className="auth-card" onSubmit={submit}><p className="eyebrow">Business onboarding</p><h2>Create your StockGuard workspace</h2><label className="field">Business name<input name="businessName" required maxLength={120}/></label><label className="field">Owner's name<input name="ownerName" defaultValue={user.displayName||''} required maxLength={100}/></label><label className="field">Business type<select name="businessType" required><option value="">Choose one</option>{['Spaza shop','Café','Restaurant','Bakery','Grocery store','Small retailer','Other'].map(x=><option key={x}>{x}</option>)}</select></label><label className="field">Business email<input name="email" type="email" defaultValue={user.email||''} required/></label><label className="field">Phone number<input name="phone" type="tel" placeholder="+27…" required/></label><label className="field">Location<input name="location" required maxLength={160}/></label><label className="field">Number of employees<input name="employeeCount" type="number" min="1" max="10000" defaultValue="1" required/></label><fieldset><legend>Expiry warning periods</legend><label className="field">Critical (days)<input name="criticalDays" type="number" min="0" max="30" defaultValue="3"/></label><label className="field">Urgent (days)<input name="urgentDays" type="number" min="1" max="60" defaultValue="7"/></label><label className="field">Expiring soon (days)<input name="soonDays" type="number" min="1" max="90" defaultValue="14"/></label><label className="field">Watch list (days)<input name="watchDays" type="number" min="1" max="180" defaultValue="30"/></label></fieldset><p className="error" role="alert">{error}</p><button className="button" disabled={busy}>{busy?'Creating workspace…':'Create business workspace'}</button></form></section></main>}
