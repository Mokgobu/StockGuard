import { useState,type FormEvent } from 'react';
import { signInWithEmailAndPassword } from 'firebase/auth';
import { Navigate } from 'react-router-dom';
import { useAuth } from '../auth/AuthProvider';
import { auth } from '../firebase/client';
export function AuthPage(){const {user}=useAuth();const [error,setError]=useState('');if(user)return <Navigate to="/" replace/>;async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');const data=new FormData(e.currentTarget);try{if(!auth)throw new Error('Firebase environment variables are not configured.');await signInWithEmailAndPassword(auth,String(data.get('email')),String(data.get('password')))}catch(err){setError(err instanceof Error?err.message:'Unable to sign in.')}}return <main className="auth-page"><section className="auth-brand"><span className="brand-mark">SG</span><p className="eyebrow">StockGuard</p><h1>Know what expires next. Protect every rand.</h1><p>SMART INVENTORY &amp; EXPIRY MANAGEMENT.</p></section><section className="auth-panel"><form className="auth-card" onSubmit={submit}><p className="eyebrow">Secure access</p><h2>Welcome back</h2><label className="field">Email<input name="email" type="email" autoComplete="username" required/></label><label className="field">Password<input name="password" type="password" autoComplete="current-password" minLength={8} required/></label><p className="error" role="alert">{error}</p><button className="button" type="submit">Sign in</button></form></section></main>}
