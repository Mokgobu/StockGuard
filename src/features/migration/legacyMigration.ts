export const LEGACY_KEY='stockguard-items-v2';
export type LegacyItem={id?:string|number;name:string;category?:string;quantity:number;unit?:string;expiry:string;supplier?:string;barcode?:string;sku?:string};
export function parseLegacyInventory(raw:string):LegacyItem[]{const parsed:unknown=JSON.parse(raw);const rows=Array.isArray(parsed)?parsed:(parsed&&typeof parsed==='object'&&Array.isArray((parsed as {items?:unknown}).items)?(parsed as {items:unknown[]}).items:[]);return rows.flatMap(value=>{if(!value||typeof value!=='object')return[];const item=value as Record<string,unknown>,name=String(item.name||'').trim(),expiry=String(item.expiry||item.expiryDate||'').trim(),quantity=Number(item.quantity);if(!name||!/^\d{4}-\d{2}-\d{2}$/.test(expiry)||!Number.isFinite(quantity)||quantity<0)return[];return[{id:typeof item.id==='string'||typeof item.id==='number'?item.id:undefined,name,category:String(item.category||''),quantity,unit:String(item.unit||'units'),expiry,supplier:String(item.supplier||''),barcode:String(item.barcode||''),sku:String(item.sku||'')}]} )}
export function migrationBackupKey(uid:string){return `${LEGACY_KEY}-backup-${uid}`}
