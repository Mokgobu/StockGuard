import { Bell,ChartNoAxesColumn,ClipboardCheck,House,Image,LogOut,PackageSearch,ScanLine,Settings,ShieldCheck,Tags,Users } from 'lucide-react';
import { NavLink,Outlet } from 'react-router-dom';
import { useAuth } from '../auth/AuthProvider';
import { MigrationAssistant } from '../features/migration/MigrationAssistant';
import { useOnlineStatus } from '../shared/useOnlineStatus';
const links=[['/',House,'Overview'],['/inventory',PackageSearch,'Inventory'],['/alerts',Bell,'Expiry alerts'],['/scanner',ScanLine,'Scanner'],['/actions',Tags,'Action centre'],['/signage',Image,'Signage'],['/tasks',ClipboardCheck,'Tasks'],['/reports',ChartNoAxesColumn,'Reports'],['/team',Users,'Team'],['/settings',Settings,'Settings'],['/owner-admin',ShieldCheck,'Platform admin']] as const;
function Navigation({mobile=false}:{mobile?:boolean}){const shown=mobile?links.slice(0,5):links;return <nav className={mobile?'mobile-nav':'nav-list'} aria-label={mobile?'Mobile navigation':'Main navigation'}>{shown.map(([to,Icon,label])=><NavLink key={to} to={to} end={to==='/'} className={({isActive})=>`nav-link${isActive?' active':''}`}><Icon size={19}/><span>{label}</span></NavLink>)}</nav>}
export function DashboardLayout(){const {user,logout}=useAuth();const online=useOnlineStatus();return <div className="shell"><MigrationAssistant/><a href="#main" className="skip-link">Skip to content</a><aside className="sidebar"><NavLink className="brand" to="/"><span className="brand-mark">SG</span><span><strong>StockGuard</strong><small>SMART INVENTORY &amp; EXPIRY MANAGEMENT.</small></span></NavLink><Navigation/><div className="sidebar-footer"><small>{user?.email}</small><button className="nav-link" onClick={logout}><LogOut size={18}/>Sign out</button></div></aside><main id="main" className="main"><Outlet/></main><Navigation mobile/><span className={`online-indicator${online?'':' offline'}`} role="status">{online?'Online':'Offline — drafts protected'}</span></div>}
